public class ConAccInsert {
    public static void Cont(list<Contact>ConList){
        list<Account>AccList = new List<Account>();
        for(Contact COn : ConList){
            Account acc = New Account();
            acc.Name = con.LastName;
          
            acc.Phone = con.Phone;
            AccList.add(acc);
        }
        insert AccList;
    }
}